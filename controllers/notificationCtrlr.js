import colors from "colors"

export const readNotification = async (req, res) => {
    
    console.log(colors.yellow("reading new notification"))

    try {
        const notificationId = req.body
    } catch (error) {
        
    }
}